%% 第二章

\chapter{相关研究与理论基础}
\chaptermark{相关研究与理论基础}

\section{无人机具身智能与低空多模态评测}

无人机具身智能研究关注智能体如何在三维空间中感知环境、理解任务并执行动作。与地面机器人相比，无人机具备更大的视野范围和更灵活的运动自由度，但也面临更剧烈的视角变化、更复杂的目标尺度变化和更强的时间连续性。近年来，低空无人机任务逐渐从传统检测、跟踪和遥感理解扩展到视觉语言导航、城市问答、目标搜索和高层任务规划等方向。

% 补充CityAVOS
AerialVLN 将视觉语言导航引入无人机场景，强调无人机在连续飞行中依据语言指令完成导航任务\cite{liu2023aerialvln}。LongFly、AeroDuo、OpenFly、SkyVLN 以及 See, Point, Fly 等工作进一步关注长时程空中导航、多无人机场景协同、统一导航平台、控制耦合导航和免训练导航框架，为多视角低空任务提供了新的研究基础\cite{jiang2025longfly,wu2025aeroduo,OpenFly,li2025skyvln,pmlr-v305-hu25e}。CityNav 和 CityNavAgent 面向真实城市空间构建大规模空中导航任务，强调层次化语义规划、全局记忆和长程路径理解\cite{lee2025citynav,zhang2025citynavagent}。进一步地，CityEQA、HUGE-Bench、LogisticsVLN、UrbanVideo-Bench、CityAVOS等工作，将具身问答、视觉语言动作任务、低空物流导航、城市视频具身评测和目标搜索代理纳入无人机多模态评测范围\cite{zhao2025cityeqa,guo2026huge,zhang2025logisticsvln,zhao2025urbanvideobench,ji2026towards}。

上述工作推动了无人机多模态任务的发展，但多数评测仍更强调环境理解、任务完成或导航结果。例如，模型往往需要判断目标在哪里、该如何到达目标，或者视频中发生了什么事件。然而，在真实无人机任务中，智能体还必须理解自身状态，例如相对地标的位置、当前朝向、未来视角和飞行行为。如果评测只考察外部环境而忽略无人机自身状态，就难以全面刻画具身推理能力。

本文的双认知框架正是针对这一不足提出。UAV-DualCog 将无人机的自身状态和外部环境并列为顶层认知对象，并在图像与视频两种媒介中同时设置任务，使评测能够覆盖“我在哪里、我会看到什么、我正在怎样飞行”和“目标在哪里、我该怎样靠近目标、目标何时可见”等问题。

\section{多模态大模型综合评测与空间推理}

近年来，GPT-5.5、Claude Opus 4.7、Qwen3.7 Max、MiMo-2.5-Pro等多模态大模型不断推动视觉语言模型在感知、理解和推理上的能力边界\cite{hurst2024gpt4o,wang2024qwen2vl,wang2025internvl3_5}。在此基础上，多模态大模型评测已经形成较丰富的任务体系。MME 从感知和认知两个方面评估多模态模型基础能力\cite{fu2023mme}；MMBench 采用选择题形式评估模型是否具备较全面的多模态理解能力\cite{liu2024mmbench}；SEED-Bench、MM-Vet、MM-Vet v2、MLLM-Bench 和 MMMU 等工作进一步考察复杂视觉问答、组合推理、跨学科理解和综合能力\cite{li2024seed,yu2023mmvet,yu2024mmvet2,ge2025mllm,yue2024mmmu}。这些基准推动了多模态大模型从感知型任务向推理型任务发展。

在空间推理方面，Thinking in Space、HiSpatial、Open3D-VQA 和 CityRefer 等工作开始关注模型对空间关系、三维结构、开放空间定位和跨视角关系的理解\cite{yang2025thinking,liang2026hispatial,zhang2025open3d,miyanishi2023cityrefer}；而面向空中智能体的统一框架也进一步强调空间、时间与具身推理的耦合\cite{xu2025aerial,yang2026vision}。这些研究说明，模型不应只识别目标“是什么”，还应进一步说明目标“在哪里”、该判断基于怎样的几何关系以及模型是否具备跨视角空间记忆。

但是，现有多数综合评测仍以静态图像或短视频为输入，视觉内容与智能体主动运动关系较弱。在这种被动观察设置中，模型即使取得较高得分，也不能直接证明其能够在无人机主动飞行、视角快速变化和目标可见性动态变化的场景中保持可靠。更重要的是，许多评测只判断答案是否正确，并不要求模型给出可检验的空间或时间证据。UAV-DualCog 因此将证据纳入正式评测：图像任务不仅要求输出选项，还要求输出目标框；视频任务不仅要求输出行为或次数，还要求输出对应时间区间。

\section{能力优化训练方法}

多模态大模型能力优化通常通过后训练方法实现。监督微调是最常见、最稳定的能力注入方式，能够利用高质量样本让模型学习任务输入到结构化输出的映射关系。LoRA 和 QLoRA 等参数高效微调方法降低了微调资源需求，使中小规模多模态模型能够在有限算力条件下完成任务适配\cite{hu2021lora,dettmers2023qlora,han2024peft}。

对于多模态结构化证据任务而言，监督微调的重要意义在于让模型学习稳定输出“答案+证据”的格式。图像任务中的多项选择答案和目标框可以共同转化为文本监督目标，视频任务中的语义结果和时间区间也可以用结构化 JSON 表达。通过这种方式，模型能够先建立基本任务格式和语义-证据对应关系。

然而，已有研究和实验现象也表明，单纯依靠监督微调未必能解决细粒度证据一致性问题。模型可能学会输出格式正确的答案，却仍不能真正依赖正确目标框或时间区间完成推理。近年来，基于可验证奖励的强化后训练方法受到关注。GRPO 等组相对策略优化方法能够在不引入复杂 critic 的情况下，利用格式奖励、答案奖励和证据质量奖励对模型进行小规模校正\cite{shao2024deepseekmath,wen2025verifiable}。

本文的能力优化部分建立在上述后训练方法基础上：先使用 UAV-DualCog-Train 对基座模型进行真实多模态监督微调，使其学习稳定的“答案+证据”结构化输出；再通过联合 GRPO 同时引入格式、语义和空间证据奖励，对语义-证据一致性进行定向优化。
% 第4章围绕这一技术路线完成训练数据组织、方法实现、正式训练核查和最终导出的 GRPO 权重模型任务级评测，并从图像任务直接收益、视频任务迁移分化和双认知能力变化三个角度分析优化效果。

% \section{本章小结}

% 本章从无人机具身智能、多模态大模型评测、空间推理和能力优化训练四个角度梳理了相关研究。现有工作已经为无人机场景、多模态评测和空间推理提供了重要基础，但仍缺少以“自我状态认知”和“环境情况认知”为顶层结构、同时要求语义结果与显式证据对齐的统一无人机评测框架。UAV-DualCog 的设计正是在这一研究空缺上展开。
